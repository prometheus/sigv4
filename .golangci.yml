formatters:
  enable:
    - gofumpt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/prometheus/sigv4
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
linters:
  enable:
    - errorlint
    - misspell
    - revive
    - testifylint
  exclusions:
    generated: strict
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
  settings:
    revive:
      rules:
        - name: unused-parameter
          severity: warning
          disabled: true
    testifylint:
      disable:
        - float-compare
        - go-require
      enable-all: true
version: "2"
